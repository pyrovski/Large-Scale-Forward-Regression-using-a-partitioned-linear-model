#include "type.h"

__device__ void matVec(const unsigned M, 
		       const unsigned N,
		       const ftype *A, 
		       const unsigned lda,
		       const ftype *x,
		       ftype *result){

}

__device__ void dot(const unsigned N, 
		    const ftype *x,
		    const ftype *y,
		    ftype *result){
}
